# IterableTiger

Requirements:

1. Within the 'assets' folder, add a 'config' folder
2. Within the 'config' folder add a file named 'Config.js'
3. Within 'Config.js' add this code and update with your Iterable API Key

```export const iterableAPIKey = '2b6f370862ca45e6922a8071ffea3ee0';```